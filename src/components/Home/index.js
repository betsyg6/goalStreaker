import React from 'react';

const Home = () => (
	<div>
		<h1>My Goals</h1>
		{/* display most recent goal and progress */}

		{/* component to display list of goals */}
		<p>View Goals</p>

		{/* component to add new goal */}
		<p>Add Goal</p>
	</div>
);

export default Home;
